# FrameSolve 3D

A browser-based 3D frame and beam solver built with React, Three.js, and Vite.

## Development

```sh
npm install
npm run dev
```

## Build

```sh
npm run build
```

The production site is built into `dist/` and deployed to GitHub Pages by the workflow in `.github/workflows/pages.yml`.
